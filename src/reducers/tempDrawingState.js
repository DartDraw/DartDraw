const initialState = {
    shape: {}
};

function tempDrawingState(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default tempDrawingState;
